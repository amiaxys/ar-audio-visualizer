Command line for local development. Make sure browser-sync is installed.
```
browser-sync start --server --no-online --files="**/*" --port=3001
```

To install browser-sync:
```
npm i -g browser-sync
```